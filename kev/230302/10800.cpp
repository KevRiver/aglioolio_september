#include <iostream>
#include <algorithm>
using namespace std;

struct Ball{
    int color;
    int size;
};

Ball balls[200001];

int N;
void solve(){
    for(int i = 0; i < N; ++i){
        int color, size;
        cin >> color >> size;
        
    }
}

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    solve();

    return 0;
}