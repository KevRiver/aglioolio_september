#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

struct Schedule{
    int start;
    int end;
};

int N;
vector<Schedule> schedules;

void solve(){

    cin >> N;
    schedules.resize(N);

    for()

    // 연속된 일정
    
}

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    solve();

    return 0;
}